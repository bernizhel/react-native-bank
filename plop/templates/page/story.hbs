import { {{pascalCase name}} } from './{{dashCase name}}';

import { storiesOf } from '@storybook/react-native';

storiesOf('ui/pages', module).add('{{pascalCase name}}', () => (
  <{{pascalCase name}} />
));
