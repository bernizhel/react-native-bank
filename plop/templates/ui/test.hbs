import type { T{{pascalCase name}}Props } from '.';

import { {{pascalCase name}} } from '.';
import { render } from '@shared/lib/testing-library';

describe('[UI] {{pascalCase name}}', () => {
  it('matches snapshot', () => {
    const { toJSON } = render(<{{pascalCase name}} />);
    expect(toJSON()).toMatchSnapshot();
  });
});
